import type { GenerateConfig, SourceItem } from "@/types";
import { BUILTIN_FIELDS } from "@/fieldSpec";
import { LIMITS } from "@/lib/limits";

const SCALE_GUIDES: Record<GenerateConfig["scale"], string> = {
  精简: "每个功能点 3~8 条，聚焦主干流程与关键异常",
  标准: "每个功能点 8~15 条，覆盖主流程、分支与常见异常/边界",
  全面: "每个功能点 15 条以上，穷举正常、边界、异常、安全、性能等维度",
};

const LANG_GUIDES: Record<GenerateConfig["lang"], string> = {
  自动识别: "使用与 PRD 文档相同的语言输出，若文档混用则以正文主体语言为准",
  中文: "全部内容使用简体中文输出",
  英文: "全部内容使用英文输出",
};

export interface BuiltPrompt {
  messages: Array<{ role: string; content: string }>;
  images: Array<{ mime: string; dataUrl: string }>;
  /** 文本总字符数（含截断提示），供统计展示 */
  textChars: number;
}

/** 当前可见且启用的字段 key 列表（决定生成内容与输出列） */
export function outputFieldKeys(config: GenerateConfig): string[] {
  return config.fields.filter((field) => field.visible && field.enabled).map((field) => field.key);
}

function enabledCustomKeys(config: GenerateConfig): string[] {
  return config.customFields
    .filter((f) => outputFieldKeys(config).includes(f.key))
    .map((f) => f.key);
}

/** 组装发给本地代理 → OpenRouter 的 messages 与图片 */
export function buildPrompt(sources: SourceItem[], config: GenerateConfig): BuiltPrompt {
  const customFieldKeys = enabledCustomKeys(config);
  const outputKeys = outputFieldLabels(config);
  const scale = SCALE_GUIDES[config.scale];
  const lang = LANG_GUIDES[config.lang];
  const testTypes = config.testTypes.join("、");

  const fieldSection = [
    "每条例用例必须包含以下字段（键名以 JSON key 为准）：",
    outputKeys.map((k) => `- ${k.key}：${k.label}`).join("\n"),
    "只生成上面字段清单中的字段；字段配置中隐藏或停用的字段禁止出现在 cases 中。",
    "id 必须输出空字符串，由本地程序统一连续编号，禁止自行编号；",
    customFieldKeys.length > 0
      ? `自定义字段（${customFieldKeys.map((k) => `custom_${k}`).join("、")}）：无内容时输出空字符串。`
      : "",
    "steps 为字符串数组，每步一条。",
  ]
    .filter(Boolean)
    .join("\n");

  const fieldRulesSection = [
    "各字段填写规则（只对本次实际存在的字段生效）：",
    "- id：输出空字符串（本地统一连续编号）。",
    "- module（模块）：填写测试点实际所属的一级功能模块，不得用整份需求文档的标题代替，不得留空。",
    "- featurePoint（功能点）：填写当前测试点所属的具体功能或父级路径，确保能够追溯到需求来源，不得留空。",
    "- title（用例标题）：标题即『测什么』，让人不看详情就知道这条用例验证什么。核心公式：【模块/功能点】+ 操作动作 + 前置条件/输入 + 预期场景。要求：见名知意、简洁不写长句、单一可判断目标、不带版本号、不掺操作步骤。按场景统一套用模板：",
    "1) 正常流程：验证【功能点】在【正常输入/操作】下业务处理正确。如：验证登录功能输入正确账号密码可成功登录；验证提交表单填写全部必填项正常提交；验证列表页正常加载全部列表数据。",
    "2) 异常场景：验证【功能点】在【异常操作/非法输入】下给出对应提示。如：验证登录输入错误密码给出密码错误提示；验证表单不填必填项点击提交校验提示；验证输入超长字符输入框做长度截断提示。",
    "3) 边界值：验证【功能点】输入边界值业务处理符合要求。如：验证输入最大长度字符输入框保存成功；验证输入最小边界数值表单校验生效。",
    "4) UI/显示：验证【页面/控件】在【某状态】展示正确。如：验证空数据场景页面展示空状态文案；验证按钮禁用状态置灰不可点击。",
    "5) 兼容性/环境：验证【XX功能】在【XX环境】运行正常。如：验证页面在低版本安卓系统页面展示无错乱。",
    "——title 反面示例（禁止）：①写『测试登录』『测表单』『验证功能是否正常』等过于笼统、看不出正常还是异常的标题；②把操作步骤写进标题（步骤一律放 steps）；③标题混杂步骤和预期结果（应改为：验证登录输入错误密码，给出密码错误提示）。",
    "——标题其它要求：同一功能的多个用例标题保持统一前缀便于筛选（如：验证搜索-输入关键字搜索结果正确 / 验证搜索-输入特殊字符给出容错提示）；接口用例可复用同套规范（如：验证XX接口传非法参数返回对应错误码）；存在不确定内容时在末尾标注『待人工确认』。",
    "- precondition（前置条件）：只填写执行用例必须满足的状态，不得包含具体操作步骤；没有必要前置条件时填空字符串。前置条件必须与操作步骤自洽：若前置为『已进入/已登录某页面』，步骤中不得再出现『打开/进入/点击进入该页面』；验证『进入某页面』与验证『该页面内容』是两种目标，须二选一拆分为独立用例，禁止在同一条用例里既做进入操作又声明已进入。",
    "- steps（操作步骤）：带序号的单步可执行操作，每步是测试人员能执行的动作；合并多种触发方式时分别列出每种方式。每一条操作后应能对应一个可判定的观察点，禁止把『查看并多次点击』『进入处理流程』等含糊动作合并成一步却无检查时点；验证多档取值（如不同时长/分辨率/大小）时，逐档选取并在每一步后单独检查选中状态或结果，不要一次填多个再一次性断言。",
    "- testData（测试数据）：测试数据必须输出实际内容，禁止输出『无』『/』『—』『不需要』『无输入』『数据暂无』等空占位值；即使某输入为常量或仅用于展示验证，也必须写出其具体内容。必须来源于需求或由需求合理推导，填写需要的输入值、文件类型、边界值、账号状态、任务状态或参数组合。纯展示/验证控件存在的用例（如『验证【某页面】展示模型、上传、描述及生成配置等控件』），不得因『只验证展示』就写『无』，应转述页面预期展示的具体内容：各控件预期的默认值/选项/支持格式/字符上限/占位文案等，例如『页面预期模型：PixVerse 6；支持格式提示：仅支持 JPG、PNG、JPEG、WEBP；描述字符上限：1500；其余控件默认值：按需求确认』。需求没有给出真实 ID、文件或账号时，不得填写『需求未提供具体测试数据』，应填写不依赖虚构具体值的语义化场景数据，例如『有效 job_id』『不存在的 job_id』『已过期的 job_id』『积分余额不足的账号』『会触发内容违规的测试素材』；只有测试场景、输入类别或参数含义都无法从需求确定时，才允许标记待人工确认。测试数据必须具体且可复现：①数值型边界要写具体数（如上限 1500 则写 1501/1500/1499），并说明计数规则（字符或字节、中文/英文/emoji/换行是否计入），禁止只写『超过 1500 个字符』『页面默认数据』『自定义时长』等占位式表述；②文件类输入必须给出文件名、格式、大小、尺寸/分辨率等可复现属性（如 test-valid.jpg、5MB、1920×1080）；③明确该输入是『选择时校验拒绝』还是『上传后校验拒绝』，并区分合理推导数据与需人工确认的产品规则。",
    "- expected（预期结果）：明确、可观察、可验证，必要时与步骤按序号对应；不得只写『功能正常』『结果正确』『符合预期』『显示正常』等不可验证描述。预期必须唯一且可判定：禁止用『或』并列两种可能结果（如『进入处理流程或展示生成中』『按钮禁用或给出提示』）；禁止『若存在…能力』『目标由产品定义』『按需求达到』等条件化、不可判定的表述——若确属需求未定，先把可确认的唯一结果写出来，再把无法确定的产品规则单独标记『待人工确认』。核心业务流程（提交/创建/生成/支付等）的预期必须给出可观测的成功标识（如任务/记录出现且状态正确、提交或随请求传递的参数与页面设置一致）与失败时明确的失败表现（如提示文案、无任务生成、按钮态变化）。需求未明确的结果不得自行推断，应保留可确认部分并明确标记待人工确认。",
    "- priority（优先级）：只分 P0/P1/P2 三档，从高到低 P0→P1→P2，禁止出现 P3 或其他值。判定规则：",
    "【P0 最高】核心业务主流程，阻断业务，上线必须全部通过，每轮必执行。判定：①主流程/核心业务链路/用户最主要使用路径；②功能失效直接导致业务跑不通、无法完成核心目标；③线上问题影响大量用户，造成资损、数据错误、页面崩溃；④本次迭代新增核心功能的主流程。示例：登录、提交订单、支付、保存核心表单、数据查询主流程、新增功能主干流程。",
    "【P1 中】分支业务、常规校验，功能重要，正式测试需执行，回归可抽样。判定：①主流程之外的分支场景、普通异常校验；②非核心辅助功能，主业务不受该功能故障影响；③普通输入校验、常规边界、普通弹窗提示；④页面普通交互，问题存在但用户仍可完成核心业务。示例：表单普通字段校验、列表筛选、普通按钮交互、次要弹窗、普通边界值。",
    "【P2 最低】边缘场景、UI 体验细节，非业务阻断，工期紧张可跳过。判定：①边缘异常、极难触发的场景；②纯 UI 展示细节、文案样式、非关键排版；③极少用户会操作的路径、兼容小众环境；④问题发生不影响业务功能，仅体验瑕疵。示例：空页面边角样式、特殊小众字符、低占比机型兼容、次要文案展示。",
    "【优先级分配控制】①一个模块的 P0 数量尽量少，P0 占比控制在 20% 以内，不要全部标 P0；②新增功能主流程给 P0、分支给 P1、UI 细节给 P2；③Bug 等级 ≠ 用例优先级（一个 UI 小 bug 是低级 bug，但覆盖它的用例可能是 P1/P2）。快速区分口诀：P0=不通过版本不能上线（核心主干）；P1=功能要测、坏了影响体验但主干还能用（分支、校验）；P2=时间不够可以放过（UI 细节、极端边缘）。P0 反例（不得标 P0）：常规格式分支（如不同文件格式的上传/解析）、纯页面展示与 UI 细节、普通配置项（开关/下拉/默认值）、常规边界校验——这些默认归 P1 或 P2；除非该功能点是核心业务主流程的一部分（如核心任务创建/提交/生成）才可给 P0。给用例标 P0 前先自问：这个用例挂了，版本是否真的不能上线？否则至少降为 P1。",
    "- caseType（用例类型）：只能填 功能测试/接口测试/性能测试/安全性测试/稳定性测试/兼容性测试 之一；存在多个目标时填写最主要的类型，没有明确非功能测试目标时优先使用功能测试或接口测试。",
    "- testType（测试类型）：按本次配置填写：${testTypes}。",
    "- status/actualResult/defectId（执行状态/实际结果/缺陷 ID）：输出空字符串（执行阶段填写）。",
    "- remark（备注）：用于『待人工确认：<具体事项>』『补充测试点』『下版本』等说明；不确定内容必须说明具体待确认事项，不能只写『待人工确认』；没有备注时填空字符串。",
  ].join("\n");

  const system = [
    "你是资深软件测试工程师。请根据用户提供的需求文档，生成标准化、可执行、可验证、可追溯的中文测试用例。",
    "输出约束：只能输出一个有效 JSON 对象。禁止输出 Markdown、代码围栏、CSV、解释、前后缀文字或思考过程。" + lang,
    "",
    "测试设计方法：针对每个测试点，根据需求特点综合使用：",
    "1. 场景法：基于完整业务流程、用户路径和状态变化设计用例；",
    "2. 等价类划分：识别有效等价类和无效等价类，选择有代表性的输入；",
    "3. 边界值分析：覆盖边界值、边界内一点和边界外一点；",
    "4. 正交分析法：当存在多个独立因素和大量组合时，选择有代表性的组合，避免组合爆炸；",
    "5. 错误推测：根据常见缺陷模式补充重复操作、快速操作、异常顺序、空值、错误格式、资源不足和操作中断等场景；",
    "6. 因果图：多个输入条件共同决定输出时，分析条件组合与结果之间的因果关系；",
    "7. 接口测试：若需求涉及接口，覆盖正常请求、异常请求、边界参数、鉴权、限流、幂等性、错误处理和状态变化，但不编造需求中没有的接口字段、状态码或业务规则；",
    "8. 非功能测试：仅在需求能够支持时补充性能、安全性、稳定性和兼容性测试。",
    "",
    "测试点覆盖规则：",
    "1. 不遗漏需求中明确描述的功能点、限制条件、状态和异常情况；",
    "2. 识别需求能够合理推导出的隐含测试点；",
    "3. 对合理推导但原需求没有直接写明的测试点，在备注中标记『补充测试点』（需求中已明确出现的任何内容都不是补充测试点，禁止误标）；",
    "4. 不得编造需求中没有给出的接口字段、业务规则、状态码、页面元素、验收标准或具体数值；",
    "5. 对需求中的『需要确认』『待确认』『下版本』『暂未确定』等内容，保留其原意，并在备注中填写『待人工确认：<具体未知点>』；不得因为某个局部信息未知，就把整条用例或整格测试数据笼统写成待人工确认；",
    "6. 如果预期结果无法从需求确定，不得自行确定；先填写能够确认的可观察结果，再只对无法确定的业务结论、具体数值或冲突点标记待人工确认。",
    "",
    "用例合并规则：",
    "1. 只有 前置条件、业务结果、优先级和用例类型 均相同，且仅触发方式不同 的测试点，才允许合并为一条用例；",
    "2. 合并后，应在测试步骤或测试数据中完整列出每种触发方式；",
    "3. 不同前置状态、不同业务结果、不同优先级或不同用例类型的测试点不得强行合并；",
    "4. 不得为了减少用例数量而损失测试点覆盖；",
    "5. 不同状态码、业务结果、状态、文件类型、边界值或操作状态的测试点必须拆成独立用例；",
    "6. 每条用例只聚焦一个核心验证目标；多个场景能够分别执行、分别判断通过或失败时，禁止压缩成一条『大而全』用例；",
    "7. 避免生成测试目的、步骤和预期结果完全相同的重复用例；",
    "8. 仅『可枚举参数取值』不同（如支持的文件扩展名 jpg/png/jpeg/webp、时长档位 5s/10s/15s、分辨率 720p/1080p）且前置、业务结果、优先级、用例类型与判定规则完全一致的测试点，应合并为一条『参数化用例』，在测试数据中列出参数集（如文件清单+各自大小/尺寸、各档时长、各分辨率），不要为每个取值各造一条结构相同的用例；仅当某参数存在差异化风险、独立校验规则或不同结果时才拆为独立用例。",
    "",
    "异常与边界场景覆盖清单（结合需求是否真正涉及对应能力，不编造需求没有的功能）:",
    "1. 上传/文件类：文件选择器是否过滤、『选择时拒绝』与『上传后拒绝』、重复或清空已选文件、损坏/空/超大/非常规后缀文件、一次多文件、弱网与上传中断；",
    "2. 提交/生成类：重复点击提交（幂等）、提交或生成过程中的中间状态、超时、服务端失败、失败后重试与按钮/状态恢复、页面离开后任务状态；",
    "3. 配置项（开关/下拉/单选/输入框）：默认值、切换后对后续提交或任务参数的影响、是否持久化；需求未声明持久化时不得臆断『刷新后保持』，也不得默认『刷新后重置』；",
    "4. 权限/资源类（仅当需求涉及）：登录态、会员/额度/次数限制、模型或功能权限、内容安全等合规限制；",
    "5. 页面退出/返回类：关闭或返回前是否二次确认、未保存内容如何处理、返回后数据保留还是清空。",
    "",
    fieldRulesSection,
    "",
    "待人工确认使用规则：",
    "1. 『待人工确认』只用于需求确实缺失、相互冲突、疑似笔误或尚未确定的业务信息，不得作为缺少真实测试账号、文件、ID 或请求样例时的通用占位文字；",
    "2. 能从用例标题、前置条件、步骤或需求路径确定输入类别和状态时，必须将其转换为语义化测试数据，不得留空，也不得整格只写『待人工确认』；",
    "3. 业务规则只有一部分未知时，只标记未知部分：例如测试数据写『时长 7s』，预期结果填写能够确认的接口行为，备注写『待人工确认：最终输出视频时长』；",
    "4. 场景明确但构造方法依赖后端、测试环境或数据准备时，应保留场景化测试数据，并把『如何稳定构造该场景』写入备注待确认。",
    "",
    "生成前检查：",
    "1. 需求中的每个测试点是否至少有一条用例覆盖；",
    "2. 正常、异常、边界和状态变化场景是否合理覆盖；",
    "3. 是否存在可以合并的重复用例；",
    "4. 用例标题是否见名知意；",
    "5. 步骤是否清晰可执行，且每一步都有可判定的观察/检查时点，没有被含糊合并的『查看并多次点击』式步骤；",
    "6. 前置条件与步骤是否自洽（声明已进入后步骤不再出现打开进入操作）；",
    "7. 预期结果是否唯一可验证（没有『或』并列两种结果、没有『若存在…』『按需达到』等条件化表述）；核心流程是否写了可观测的成功/失败标识；",
    "8. 是否存在仅参数不同却重复的用例，已按参数化合并；",
    "9. 测试数据是否具体且可复现（字段名/大小/数量/边界具体值），没有『无』『/』『—』『不需要』『数据暂无』等空占位值，也没有『页面默认数据』『超过上限』『自定义XX』等占位写；",
    "10. 补充测试点是否已标记；",
    "11. 待人工确认是否只标记了具体未知点，没有把可描述的测试场景笼统替换成待人工确认；",
    "12. 优先级是否符合 P0/P1/P2 定义（P0 控制在 20% 以内，格式分支/展示类/配置项不得标 P0），相似风险使用一致优先级；",
    "13. 完成检查后，只输出符合约定的 JSON 对象。",
    "",
    "其他约束：",
    `用例规模：${config.scale}（${scale}）`,
    "仅输出 JSON；不要输出任何解释、markdown 代码块标记或多余文字。",
  ]
    .filter(Boolean)
    .join("\n");

  const coverageSection = [
    "metrics：",
    "- coverage：记录每个「功能点 × 测试类型」的用例数量、覆盖状态（已覆盖/部分覆盖/未覆盖）与风险等级（低/中/高）；",
    "- risksAndAssumptions：生成过程中的风险与假设（标注『假设值，需确认』）；",
    "- confirmations：所有待确认项（需求缺失/冲突/未确定的业务信息），每条含问题描述、影响、来源位置、建议确认内容；",
    "- modelUsed：固定返回 \"${config.model}\"。",
  ].join("\n");

  const body = buildBody(sources);

  const user = [
    "请根据以下需求内容与配置生成测试用例。",
    `测试类型：${testTypes}`,
    fieldSection,
    coverageSection,
    "输出 JSON 结构（顶层 keys 必须完整，cases 中每条的 id 为 \"\"；cases 只能包含字段清单中的字段）：",
    `{ "cases": [{ ${outputKeys.map((k) => `${k.key}${k.key === "id" ? ': ""' : ""}`).join(", ")} }], "coverage": [{ feature, testType, count, coverageStatus, riskLevel }], "risksAndAssumptions": ["..."], "confirmations": [{ problem, impact, sourceLocation, confirmSuggestion }], "modelUsed": "..." }`,
    "====== 需求内容 ======",
    body.text,
  ].join("\n\n");

  return { messages: [{ role: "system", content: system }, { role: "user", content: user }], images: body.images, textChars: body.text.length };
}

export function buildBody(sources: SourceItem[]): { text: string; images: Array<{ mime: string; dataUrl: string }> } {
  const sections: string[] = [];
  const images: Array<{ mime: string; dataUrl: string }> = [];
  let chars = 0;

  for (const s of sources) {
    if (s.status !== "success") continue;
    // 扫描件 PDF：页面图片（跟随来源原始顺序）
    if (s.kind === "pdf" && s.pageImages && s.pageImages.length > 0 && s.forceAsImage) {
      for (let i = 0; i < s.pageImages.length; i++) {
        images.push({ mime: "image/jpeg", dataUrl: s.pageImages[i] });
        sections.push(`[来源：${s.name} - 图片第 ${i + 1} 页]`);
      }
      continue;
    }
    if (s.kind === "pdf" && s.pageImages && s.pageImages.length > 0 && !s.text) {
      for (let i = 0; i < s.pageImages.length; i++) {
        images.push({ mime: "image/jpeg", dataUrl: s.pageImages[i] });
      }
      sections.push(`[来源：${s.name}（扫描件，共 ${s.pageImages.length} 页图片，按图片顺序视为页码）]`);
      continue;
    }
    if (s.kind === "image" && s.image) {
      images.push({ mime: mimeFromDataUrl(s.image), dataUrl: s.image });
      sections.push(`[来源：${s.name}（截图）]`);
      continue;
    }
    if (s.text && s.text.length > 0) {
      let t = s.text;
      // 超长保护：截断并提示
      const remaining = LIMITS.maxTextChars - chars;
      if (t.length > remaining) {
        t = t.slice(0, Math.max(0, remaining));
        sections.push(`[来源：${s.name}（内容过长，已截断至前 ${t.length.toLocaleString()} 字符，超出部分忽略）]`);
      } else {
        sections.push(`[来源：${s.name}]`);
      }
      chars += t.length;
      sections.push(`\`\`\`\n${t}\n\`\`\``);
    } else if (s.error) {
      sections.push(`[来源：${s.name} 解析失败，已跳过：${s.error}]`);
    }
  }

  return { text: sections.join("\n\n"), images };
}

function mimeFromDataUrl(dataUrl: string): string {
  return dataUrl.slice(5, dataUrl.indexOf(";")) || "image/png";
}

/** 输出字段清单：按字段配置顺序返回当前可见且启用的字段 */
export function outputFieldLabels(config: GenerateConfig): Array<{ key: string; label: string }> {
  const definitions = new Map([...BUILTIN_FIELDS, ...config.customFields].map((field) => [field.key, field]));
  return config.fields
    .filter((field) => field.visible && field.enabled)
    .map((field) => {
      const definition = definitions.get(field.key);
      return {
        key: field.key.startsWith("cf_") ? `custom_${field.key}` : field.key,
        label: definition?.label ?? field.key,
      };
    });
}
